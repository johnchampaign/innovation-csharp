<UserControl x:Class="Innovation.Wpf.CardSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="22"
             MinWidth="120">
    <!--
        One-line summary. VB6 renders "1-Agriculture" (age-hyphen-
        title) as a single dark-text phrase on a colored bar, with
        a small bare icon on the far right. Used in hand, score,
        opponent piles, and covered-card lists.
    -->
    <Border x:Name="Root"
            CornerRadius="2"
            BorderThickness="1"
            BorderBrush="#55000000"
            SnapsToDevicePixels="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- "<age>-<title>" as a single phrase -->
            <TextBlock x:Name="LabelText"
                       Grid.Column="0"
                       FontFamily="Segoe UI"
                       FontWeight="SemiBold"
                       FontSize="12"
                       Foreground="#1F2937"
                       VerticalAlignment="Center"
                       Margin="6,0,6,0"
                       TextTrimming="CharacterEllipsis"/>

            <!-- Bare icon on the right (no colored tile) -->
            <Grid x:Name="IconHost"
                  Grid.Column="1"
                  Width="16" Height="16"
                  Margin="0,0,6,0"
                  VerticalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
